sudo: required

language: node_js
node_js:
  - "node"

services:
  - docker

branches:
  only:
    - production
    - master
    - bibek/ci # TODO: Remove once done testing

deploy:
  - chmod +x deploy.sh
  - ./deploy.sh
